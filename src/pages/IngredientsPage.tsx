import { ContentIngredients } from "components/Contents/ContentIngredients";
import { Layout } from "components/Layout";

export const IngredientsPage = () => {
  return <Layout children={<ContentIngredients />} />;
};
