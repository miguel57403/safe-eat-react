import { ContentProducts } from "components/Contents/ContentProducts";
import { Layout } from "components/Layout";

export const ProductsPage = () => {
  return <Layout children={<ContentProducts />} />;
};
