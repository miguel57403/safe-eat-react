import { styled } from "styled-components";

export const StyledNotification = styled.div`
  width: 13%;
  height: 100%;

  .title-notification {
    margin: 10px 0;
  }
  .card-notification {
    display: flex;
    gap: 10px;
    flex-direction: column;
  }
`;
